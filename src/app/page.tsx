// Update the import path below if the file is located at 'src/IntegratedAlarmDashboard.tsx'
import IntegratedAlarmDashboard from '../IntegratedAlarmDashboard';

export default function HomePage() {
  return <IntegratedAlarmDashboard />;
}
